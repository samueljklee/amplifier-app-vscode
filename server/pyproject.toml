[project]
name = "amplifier-vscode-server"
version = "0.1.0"
description = "Backend server for Amplifier VS Code extension"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.109.0",
    "uvicorn>=0.27.0",
    "pydantic>=2.5.0",
    "sse-starlette>=3.0.3",
    # Amplifier ecosystem - foundation provides everything needed
    "amplifier-foundation>=0.1.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "httpx>=0.26.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.sources]
amplifier-foundation = { git = "https://github.com/microsoft/amplifier-foundation", branch = "main" }

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
